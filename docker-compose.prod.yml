version: "3"
services:
  graphql:
    build:
      context: ./server/
      dockerfile: Dockerfile.prod
    command: npm start
    ports:
      - "3030:3030"
    restart: always
    env_file: ./server/.env.production.local

  web:
    build: .
    ports:
      - "80:80"
    restart: always
